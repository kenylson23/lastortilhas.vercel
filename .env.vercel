# Las Tortilhas - Environment Variables para Vercel
# Configure estas variáveis no dashboard do Vercel (Settings → Environment Variables)

# === DATABASE (escolha uma opção) ===
# Vercel Postgres (recomendado)
POSTGRES_URL=postgresql://user:password@host:port/database

# Supabase (alternativa)
# SUPABASE_DATABASE_URL=postgresql://user:password@host:port/database

# === SYSTEM ===
NODE_ENV=production
VERCEL=1

# === DATABASE OPTIMIZATION (opcional) ===
DB_POOL_SIZE=1
DB_POOL_MIN=0
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=10000

# === VERCEL SPECIFICS (automático) ===
# VERCEL_URL - configurado automaticamente pelo Vercel
# VERCEL_ENV - configurado automaticamente pelo Vercel