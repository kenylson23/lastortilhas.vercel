# Railway Environment Variables Example
# O Railway configurará automaticamente estas variáveis

# Porta do servidor (configurada automaticamente pelo Railway)
PORT=5000

# Banco de dados PostgreSQL (configurado automaticamente pelo Railway)
DATABASE_URL=postgresql://usuario:senha@host:5432/banco
DATABASE_PRIVATE_URL=postgresql://usuario:senha@host:5432/banco

# Ambiente de produção
NODE_ENV=production

# URLs do domínio (configuradas automaticamente)
RAILWAY_PUBLIC_DOMAIN=https://seuapp.railway.app