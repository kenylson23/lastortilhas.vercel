# Database Configuration (Railway will provide this automatically)
DATABASE_URL=postgresql://user:password@host:port/database
DATABASE_PRIVATE_URL=postgresql://user:password@host:port/database

# Node Environment
NODE_ENV=production

# Port (Railway will set this automatically)
PORT=3000

# Optional: Supabase fallback (if you want to use Supabase instead)
# SUPABASE_DATABASE_URL=postgresql://user:password@host:port/database