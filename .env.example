# Las Tortilhas - Configuração de Environment Variables
# Copie este arquivo para .env e configure suas variáveis

# === DATABASE CONFIGURATION ===
# Use apenas UMA das opções abaixo baseado na sua plataforma:

# Replit (automático)
# DATABASE_URL=postgresql://user:password@localhost:5432/database

# Railway
# DATABASE_PRIVATE_URL=postgresql://user:password@host:port/database
# DATABASE_URL=postgresql://user:password@host:port/database

# Supabase
# SUPABASE_DATABASE_URL=postgresql://user:password@host:port/database

# Vercel Postgres
# POSTGRES_URL=postgresql://user:password@host:port/database

# Neon Database
# NEON_DATABASE_URL=postgresql://user:password@host:port/database

# === SERVER CONFIGURATION ===
# PORT=5000
# HOST=0.0.0.0
# NODE_ENV=development

# === DATABASE POOL CONFIGURATION (Opcional) ===
# DB_POOL_SIZE=10
# DB_POOL_MIN=1
# DB_IDLE_TIMEOUT=30000
# DB_CONNECTION_TIMEOUT=5000

# === LOGGING (Opcional) ===
# ENABLE_LOGGING=true

# === AUTHENTICATION (Replit Auth automático) ===
# As variáveis de autenticação são configuradas automaticamente pelo Replit

# === PLATFORM SPECIFIC (Automático) ===
# Estas variáveis são definidas automaticamente pelas plataformas:
# RAILWAY_ENVIRONMENT, VERCEL, RENDER, HEROKU_APP_NAME, 
# REPLIT_DEPLOYMENT_ID, NETLIFY, FLY_APP_NAME