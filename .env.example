# ============================================
# Las Tortilhas - Environment Configuration
# ============================================

# Database Configuration (Required)
# For Supabase: postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
# For local PostgreSQL: postgresql://user:password@localhost:5432/dbname
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Session Security (Required)
# Generate a strong random string with at least 32 characters
# You can use: openssl rand -base64 32
SESSION_SECRET=your-super-secret-session-key-min-32-characters-generate-random

# Deployment Environment
NODE_ENV=production

# ============================================
# Optional: Replit Authentication (if used)
# ============================================
# REPLIT_CLIENT_ID=your-replit-client-id
# REPLIT_CLIENT_SECRET=your-replit-client-secret

# ============================================
# Vercel Deployment Notes:
# ============================================
# 1. Set these variables in Vercel Dashboard:
#    Settings > Environment Variables
# 2. Apply to: Production, Preview, Development
# 3. DATABASE_URL must point to your Supabase instance
# 4. SESSION_SECRET should be unique and secure