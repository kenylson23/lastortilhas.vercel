{
  "version": 2,
  "functions": {
    "api/index.ts": {
      "runtime": "@vercel/node@3",
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/dist/client/$1"
    }
  ],
  "installCommand": "npm install",
  "buildCommand": "vite build",
  "outputDirectory": "dist/client",
  "env": {
    "NODE_ENV": "production"
  }
}